# CLAUDE.md

이 리포지터리에서 작업할 때 반드시 따를 것.

---

## 0. 이 리포지터리는 보통 리포지터리가 아니다

**빌드 통과가 목표가 아니다.**

보통의 코드베이스에서는 컴파일되지 않는 코드가 버그다. 여기서는 다르다. 이곳은 경제 위기 해석 프레임워크를 Lean으로 형식화해 **정의의 구멍을 드러내는 것**이 목적인 연구 리포지터리다.

증명되지 않는 진술은 고칠 버그가 아니라 **산출물**이다. 그것이 이 프로젝트가 찾는 것이다.

따라서 빌드를 통과시키려고 진술을 손대는 순간 프로젝트의 목적이 무너진다. 아래 §1이 그래서 존재한다.

---

## 1. 절대 규칙

### 1-1. 진술을 바꾸지 마

`theorem` 과 `def` 의 **진술(statement)** 을 바꾸지 않는다. 여기에는 다음이 포함된다.

- 가정 추가 — 통과시키려고 `h : ...` 를 덧붙이지 않는다
- 결론 약화 — `=` 를 `≤` 로, 전칭을 존재로 바꾸지 않는다
- 타입 변경 — `ℤ` 를 `ℚ` 로 바꾸지 않는다
- 시그니처의 암묵 인스턴스 추가

**증명이 막히면 그 자리에 `sorry` 를 두고 보고한다.** `sorry` 는 작업 중 허용된다. 진술을 바꿔 통과시키는 것은 허용되지 않는다.

자유롭게 해도 되는 것: import 경로, 보조정리 이름, 전술 선택, 증명 구조, `deriving` 절.

**예외:** 사람이 명시적으로 "이 진술을 이렇게 바꿔라"라고 지시한 경우. 그때도 무엇을 어떻게 바꿨는지 보고한다.

### 1-2. 금지 선언

- `axiom` — 예외 없음. Lean은 공리 집합의 무모순성을 검사하지 않으므로 공리 선언은 검증 외관만 만든다
- `Classical.choice`, `Classical.em`, `open Classical` 직접 사용
- `noncomputable`

배중률이 필요하면 `Decidable` 인스턴스를 제공하는 쪽으로 간다. 그것도 안 되면 진행하지 말고 보고한다.

### 1-3. 핀을 바꾸지 마

```
Mathlib 태그      v4.32.2
Mathlib 커밋      905b95818eb32af7874a58b427f50c1711a5e96c
lean-toolchain    leanprover/lean4:v4.32.2
```

`lakefile.toml`, `lean-toolchain`, `lake-manifest.json` 을 수정하지 않는다. 필요한 정리가 이 버전에 없으면 **우회하거나 보고한다.** 버전을 올리지 않는다.

핀 갱신은 공리 기준선 전수 재감사를 동반하므로 사람의 결정 사항이다.

`lake exe cache get` 이 실패하면 **로컬 전체 빌드로 넘어가지 말고 보고한다.** Mathlib 전체 빌드는 몇 시간이 걸리고, 캐시 미스는 대개 다른 문제의 신호다.

### 1-4. 판단하지 마

이 리포지터리의 설계 결정은 사람이 한다. 검사 결과가 나오면 **관측값을 보고하고 멈춘다.**

- 공리 감사 결과를 보고 수 체계를 바꾸지 않는다
- 검사 결과를 보고 파일을 이동하거나 구조를 바꾸지 않는다
- "이렇게 하는 게 나을 것 같아서" 하지 않는다

해석과 권고는 보고서에 적어도 되지만, **실행하지 않는다.**

---

## 2. 층 구조

디렉터리가 층을 결정한다. 파일을 다른 층으로 옮기지 않는다.

```
CrisisFramework/
  Glossary/      개념의 존재와 관계만. 성질 없음. 수 없음
  Definition/    무차원. 시점 인덱스 ℕ 만 예외
  Accounting/    ℤ 기본. ℚ 는 강제될 때만
  Dynamics/      ℝ 허용
  Observation/   값 없음. 측정 사상의 명세만. 수 없음
```

**import 방향은 단방향이다.**

```
Observation → Dynamics → Accounting → Definition → Glossary
```

역방향 import는 위반이다. 순환은 물론이다.

**층별 금지:**

| 층 | 금지 |
|---|---|
| `Glossary/` | 성질을 갖는 정의, 수치 타입 |
| `Definition/` | `ℚ` `ℤ` `ℝ` 등 구체 수 타입 (`ℕ` 시점 인덱스만 예외) |
| `Accounting/` | `ℝ`. `ℚ` 는 사유 기록 없이 도입 금지 |
| `Observation/` | 수치 리터럴, 수 타입 |

---

## 3. docstring 의무

모든 `def` 와 `theorem` 은 docstring을 갖는다. 아래 항목을 **지우거나 줄이지 않는다.**

`def` 필수 3항목:
1. 대응하는 비형식 개념
2. **이 정의가 배제하는 구체 사례** (실제 역사 사례여야 함)
3. 검토했다 기각한 대체 정의와 사유

`theorem` 필수:
- **"이 명제가 거짓이려면 무엇이 관측되어야 하는가"**

이 항목들은 프로젝트의 1차 산출물이다. 리팩터링 중 유실되지 않게 한다.

---

## 4. 식별자 규칙

- 식별자는 **영어**, docstring은 **한국어**
- 정리명은 Mathlib 관례 (snake_case, 진술의 모양에서 유도)
- **아래 어간을 단독으로 쓰지 않는다.** 반드시 한정어를 동반한다

```
liquidity  leverage  capital  risk  collateral
margin     money     funding  intermediation
```

`liquidity` 금지, `liquidityBalanceSheetCapacity` 허용. 이 어간들은 원 문헌에서 다의어임이 확인되었다.

---

## 5. 검증 의무

작업 후 `bash scripts/verify.sh` 를 돌린다. 개별 항목은 아래와 같다.

### 5-1. 빌드

`lake build` — error 0, warning 0.

warning을 남기지 않는다. 미사용 가정 경고는 특히 중요하다. 불필요한 가정은 정리를 약하게 만든다.

### 5-2. 공리 기준선 감사

모든 주요 정리에 `#print axioms` 를 돌린다.

**화이트리스트가 아니라 기준선 차분이다.** 현재 기준선은 그 층이 쓰는 최소 수 체계만 언급하는 자명한 정의의 출력이다.

```
Accounting/ (ℤ)    [propext, Quot.sound]
Accounting/ (ℚ)    [propext, Classical.choice, Quot.sound]
```

기준선을 초과하는 공리가 나타나면 **어느 보조정리에서 유입됐는지 추적해 보고한다.** 추적 없이 통과시키지 않는다.

초과분의 발원지를 세 가지로 분류해 보고한다.

| 종류 | 예 | 성격 |
|---|---|---|
| 수 표현의 의존 | `Rat` 의 기약성 증명 | 계산 가능성을 해치지 않음 |
| 전술 구현의 의존 | `linarith` (`omega` 는 깨끗) | 전술 교체로 회피 가능 |
| 논증의 의존 | `ℝ` 위의 고전적 존재 증명 | 실제 고전논리 사용 |

### 5-3. 변이 검사

**모든 진술**(`theorem`, `example`)에 대해 결론을 부정한 사본이 컴파일에 실패하는지 확인한다.

두 가지 다른 것을 잡는다.

- `example` — 이름이 없어 elaboration 누락이 눈에 안 보인다. 실제로 검사됐는지 확인
- 명명된 정리 — 전술이 아무거나 증명하고 있지 않은지 확인

변이 사본은 원본을 건드리지 않고 임시 파일에 만든다. 검사 후 삭제한다.

### 5-4. 음성 대조

**검사 스크립트가 공허하지 않은지 확인한다.**

각 검사 단계에 대해, 그 단계가 잡아야 할 위반을 일부러 주입했을 때 실패하는지 확인한다. 통과만 보고 스크립트가 동작한다고 판단하지 않는다.

주석 안의 문자열이 코드로 오인되지 않게 한다. 검사는 주석 제거 후 수행한다.

### 5-5. 사본 검사

빌드 타깃과 이름이 겹치는 사본 파일이 리포지터리 어디에도 없어야 한다. 각 모듈은 단 하나의 경로로만 존재한다.

---

## 6. 스크래치

검사·탐색용 코드는 `Scratch/` 에 둔다. 빌드 타깃 밖이다.

```
lake env lean Scratch/Foo.lean
```

**검증된 파일을 검사 목적으로 수정하지 않는다.** 스크래치에 사본을 만들어 거기서 실험한다.

---

## 7. 보고 형식

작업이 끝나면 아래를 보고한다.

1. **빌드 결과** — 파일별 통과 / `sorry` 남음 / 실패
2. **변경 내역** — 무엇을 왜 바꿨는지. import, 보조정리 이름, 전술 각각
3. **공리 감사** — 정리별 `#print axioms` 출력. 기준선 초과분이 있으면 발원지 추적과 §5-2 분류
4. **변이 검사** — 몇 개 진술을 검사했고 전부 거부됐는지
5. **진술을 바꿔야만 통과할 것 같은 지점** — 있으면 바꾸지 말고 지점과 이유만
6. **판단이 필요하다고 보이는 것** — 실행하지 말고 제시만

보고에 **관측값과 해석을 섞지 않는다.** 관측값을 먼저 그대로 적고, 해석은 따로 표시한다.

---

## 8. 커밋

- 한 커밋에 한 가지 변경
- 검증 통과 상태만 커밋
- 커밋 메시지에 무엇이 검증됐는지 적는다
- `BUILD_LOG.md` 를 갱신한다. 사실과 어긋난 상태로 두지 않는다

---

## 9. 요약 — 헷갈리면 이것만

**증명되지 않는 진술은 고칠 것이 아니라 보고할 것이다.**

**검사 결과는 보고할 것이지 행동할 근거가 아니다.**