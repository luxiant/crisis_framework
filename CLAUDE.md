# CLAUDE.md

이 문서는 **리포지터리 안에서 집행되는 것**(행동 규율 · 층 경계 · docstring 보존 의무 · 검증
의무 · 보고 형식)을 규정한다. **값**(공리 기준선 · 툴체인 핀 · 오염 어간 목록)은
`docs/baseline.md`가 규정하고, **판단이 필요한 것**(무엇을 형식화할지 · 진술을 어떻게 쓸지 ·
구멍을 어떻게 분류할지)은 결정 세션의 세션 지시가 규정한다.

어느 쪽에 쓸지는 **그 문면을 읽어야 집행되는 주체**로 판별한다.

> 이 문서의 각 절 끝에 붙은 `← ID` 표기는 그 절이 딛는 규율 조항이다. 세션 지시가 그 조항의
> 정본을 들고, 이 문서는 집행 문면을 든다. **역추적되지 않는 절은 이 문서에 둘 수 없다.**
>
> `← 자체` 는 이 문서가 그 조항의 정본이라는 뜻이다. 순수 집행 관례처럼 판단을 요구하지 않는
> 문면은 세션 지시로 올리면 전사가 되므로 여기가 원천이다. **빈 칸은 미확인이고 `← 자체` 는
> 확인된 자기 정본이라, 둘을 구별해야 감사가 성립한다.**

---

## 0. 이 리포지터리는 보통 리포지터리가 아니다

**빌드 통과가 목표가 아니다.**

보통의 코드베이스에서는 컴파일되지 않는 코드가 버그다. 여기서는 다르다. 이곳은 경제 위기 해석
프레임워크를 Lean으로 형식화하여 **정의의 구멍을 드러내는 것**이 목적인 연구 리포지터리다.

증명되지 않는 진술은 고칠 버그가 아니라 **산출물**이다. 그것이 이 프로젝트가 찾는 것이다.

따라서 빌드를 통과시키려고 진술을 손대면 그 순간 프로젝트의 목적이 무너진다. 아래 §1이 그래서
존재한다. `← P-1, P-5`

---

## 1. 절대 규칙

### 1-1. 진술을 바꾸지 마

`theorem`과 `def`의 **진술(statement)** 을 바꾸지 않는다. 진술은 결정 세션이 쓰고 이 리포에서
집행하는 것은 증명이다. 여기에는 다음이 포함된다.

- 가정 추가. 통과시키려고 `h : ...`를 덧붙이지 않는다
- 결론 약화. `=`를 `≤`로 바꾸거나 전칭을 존재로 바꾸지 않는다
- 타입 변경. `ℤ`를 `ℚ`로 바꾸지 않는다
- 시그니처에 암묵 인스턴스를 더하는 것

**증명이 막히면 그 자리에 `sorry`를 두고 보고한다.** `sorry`는 작업 중에 허용되고 main 병합만
차단한다. 진술을 바꾸어 통과시키는 것은 허용되지 않는다.

자유롭게 정해도 되는 것은 import 경로와 보조정리 이름과 전술 선택과 증명 구조와 `deriving`
절이다.

**예외.** 사람이 "이 진술을 이렇게 바꾸어라"라고 명시적으로 지시한 경우에만 바꾸며, 그때에도
무엇을 어떻게 바꾸었는지 보고한다. `← W-1, A-5`

### 1-2. 금지 선언

- `axiom`. 예외가 없다. Lean은 공리 집합의 무모순성을 검사하지 않으므로 공리 선언은 검증의
  외관만 만든다
- `Classical.choice` · `Classical.em` · `open Classical`의 직접 사용
- `noncomputable`

**배중률이 필요해 보이면 `Decidable` 인스턴스를 제공하는 쪽으로 가지 말고, 타입 선택으로
피할 수 있는지 먼저 본다.** 판정 결과를 `Bool` 반환 계산 함수로 주면 `Decidable` 인스턴스를
선언할 일 자체가 없어지고, 판정은 `Bool`의 `DecidableEq`에서 나온다.

인스턴스 제공이 답이 아닌 경우가 실측으로 확인됐다. Lean core의 `Rat`은 문제가 판정가능성에
있지 않고 `Rat.normalize` 안에 있어서, `Decidable` 인스턴스를 아무리 얹어도 해소되지 않았다.

타입 선택으로도 피할 수 없으면 **진행하지 말고 보고한다.** `← A-1, C-1, C-2, C-3, C-10`

### 1-3. 핀을 바꾸지 마

핀의 값은 `docs/baseline.md` §1이 든다. 이 문서는 값을 다시 적지 않는다.

`lakefile.toml`과 `lean-toolchain`과 `lake-manifest.json`을 수정하지 않는다. 필요한 정리가 이
판에 없으면 **우회하거나 보고한다.** 판을 올리지 않는다.

핀 갱신은 공리 기준선 전수 재감사를 동반하므로 사람의 결정 사항이다.

`lake exe cache get`이 실패하면 **로컬 전체 빌드로 넘어가지 말고 보고한다.** Mathlib 전체
빌드는 몇 시간이 걸리고, 캐시 미스는 대개 다른 문제의 신호다. `← A-6`

### 1-4. 판단하지 마

이 리포지터리의 설계 결정은 사람이 한다. 검사 결과가 나오면 **관측값을 보고하고 멈춘다.**

- 공리 감사 결과를 보고 수 체계를 바꾸지 않는다
- 검사 결과를 보고 파일을 옮기거나 구조를 바꾸지 않는다
- "이렇게 하는 편이 나아 보여서" 하지 않는다

해석과 권고는 보고서에 적어도 되지만 **실행하지 않는다.**

**기준선 초과분의 3분류도 판정이 아니라 관측의 보고다.** 어느 종류인지는 결정 세션이 정하며,
이 리포에서 하는 일은 유입원을 특정해 그대로 적는 것까지다. `← W-1, C-8`

### 1-5. 지시서 없이 착수하지 마

이 리포의 작업 단위는 **결정 세션이 준 지시서**다. 지시서는 리포에 살지 않고 호출 프롬프트로
전달된다. 지시서가 없으면 작업 카드가 없다는 뜻이므로 구현에 착수하지 않는다.

**`BUILD_LOG.md` 와 `arcs.json` 과 원장은 무엇이 있었는지를 드는 기록이지 지시서가
아니다.** 거기서 다음 작업을 유도하지 않는다. `arcs.json` 이 페이즈 목록을 들고 있더라도 그것은
계획이지 작업 카드가 아니다. `← W-7`

### 1-6. 원장을 고치지 마

`docs/decisions/` 는 낙착의 정본이며 **수정 불가**다. 읽는 것은 자유이나 항목을 고치거나 지우지
않는다. 폐기조차 기존 항목을 건드리지 않고 폐기 레코드를 새로 쓰는 방식으로 하며, 그 레코드는
결정 세션이 쓴다.

`-- DD:<id>` 표지가 가리키는 항목이 없거나 폐기된 것으로 보이면 **고치지 말고 보고한다.** 표지를
지우는 것도 고치는 것이다.

**예외는 하나다.** 지시서가 카드 병합을 지시하면 그때만 원장 파일에 쓴다. 쓰는 것은 카드
파일이 든 문면 그대로이며 한 글자도 바꾸지 않는다. 병합 절차는 §14가 든다.

`arcs.json` 도 같다. 아크와 페이즈의 값 정본이며 결정 세션이 쓴다. `← R-6, R-9`

---

## 2. 층 구조

디렉터리가 층을 결정한다. 파일을 다른 층으로 옮기지 않는다.

```
CrisisFramework/
  Glossary/      개념의 존재와 관계만. 성질 없음. 수 없음
  Definition/    무차원. 시점 인덱스 ℕ 만 예외
  Accounting/    ℤ 기본. ℚ 는 강제될 때만
  Dynamics/      ℝ 허용
  Observation/   값 없음. 측정 사상의 명세만. 수 없음
```

**import 방향은 단방향이다.**

```
Observation → Dynamics → Accounting → Definition → Glossary
```

역방향 import는 위반이고 순환은 물론이다. 용어집은 다섯 번째 층이 아니라 네 층 전체의 선행
조건이므로 맨 아래에 온다.

**층별 금지:**

| 층 | 금지 |
|---|---|
| `Glossary/` | 성질을 갖는 정의, 수치 타입 |
| `Definition/` | `ℚ` `ℤ` `ℝ` 등 구체 수 타입 (`ℕ` 시점 인덱스만 예외) |
| `Accounting/` | `ℝ`. `ℚ`는 사유를 docstring에 적지 않고 도입 금지 |
| `Observation/` | 수치 리터럴, 수 타입 |

**회계층에서 비율은 교차곱 정수 부등식으로 쓴다.** 분모를 양변에 곱해 나눗셈을 피하며, 비율
표현이 필요하면 유도 표현으로 둔다. 원 문헌의 원시 조건이 이미 곱셈 부등식이므로 이것이
형식화 편의가 아니라 대상의 성질이다.

**회계층에서 통화 간 환산을 금지하고 항등식을 총액 기준으로 유지한다.** 순액으로 닫는 것을
금지한다.

**정의층의 최상위 대상은 시점 인덱스를 갖는다.** 위 표가 `ℕ` 을 예외로 허용하는 것은 인덱스를
써도 된다는 뜻이 아니라 **써야 한다**는 뜻이다. 인덱스 없는 정의가 정의층에 서면 위반이다. 노드
집합과 위계상 거리와 통화의 자금조달 지위가 전부 시점에 따라 변하기 때문이다.

**관측층은 값을 담지 않고 사상의 명세만 담는다.** 위 표의 관측층 행이 그것을 강제한다.
`← L-4, L-9, L-10, L-11, L-12, L-13, L-14, L-15`

---

## 3. docstring 의무

모든 `def`와 `theorem`은 docstring을 갖는다. 아래 항목을 **지우거나 줄이지 않는다.**

`def` 필수 3항목:

1. 대응하는 비형식 개념
2. **이 정의가 배제하는 구체 사례.** 실제 역사 사례여야 한다
3. 검토했다 기각한 대체 정의와 그 사유

`theorem` 필수:

- **"이 명제가 거짓이려면 무엇이 관측되어야 하는가"**

**이 항목들은 프로젝트의 1차 산출물이며 결정 세션이 쓴다.** 리팩터링 중에 유실되지 않게 한다.
비어 있거나 부실해 보여도 채우지 말고 보고한다. 2항의 실제 역사 사례는 논문 뭉치를 읽은 쪽만
쓸 수 있으므로, 여기서 지어내면 그것이 그대로 거짓 근거가 된다.

**분할의 완전성을 docstring에서 주장하는 경우에는 그것이 정의만으로 따라 나오는 귀결임을
밝히고 어느 귀결인지 적는다.** 회계 항등식의 귀결과 사건 대수의 귀결 둘이 있다. 이 문면도
결정 세션이 쓴다. `← D-1, D-2, D-3, T-2, V-7`

---

## 4. 식별자 규칙

- 식별자는 **영어**로 쓰고 docstring과 주석은 **한국어**로 쓴다
- 정리명은 Mathlib 관례를 따른다. snake_case이고 진술의 모양에서 유도하며 결론이 뒤에 온다
- **오염 어간을 단독으로 쓰지 않는다.** 반드시 한정어를 동반한다

**어간 목록의 정본은 `docs/baseline.md` §3이다.** 이 문서는 목록을 다시 적지 않는다. 목록이 두
곳에 있으면 한쪽을 고칠 때 다른 쪽이 조용히 낡는다.

- **원장 ID는 이름이 아니라 `-- DD:<id>` 표지로 둔다.** 정리명이나 정의명에 번호를 박지 않는다
  `← D-5, R-6`

---

## 5. 검증 의무

작업 후 `bash scripts/verify.sh`를 돌린다. 이 스크립트는 CI에서도 돈다.

**CI 워크플로 파일을 고쳐 통과시키지 않는다.** 검사가 실패하면 그 실패가 산출물이다. 워크플로
수정이 필요해 보이면 실행하지 말고 보고한다. `← §3.10-2`

### 5-1. 빌드

`lake build`를 돌려 error 0 warning 0을 확인한다.

warning을 남기지 않는다. 미사용 가정 경고가 특히 중요하다. 불필요한 가정은 정리를 약하게
만들기 때문이다. `← 자체`

### 5-2. 공리 기준선 감사

모든 주요 정리에 `#print axioms`를 돌린다.

**화이트리스트가 아니라 기준선 차분이다.** 기준선의 값은 `docs/baseline.md` §2가 든다.

기준선을 초과하는 공리가 나타나면 **어느 곳에서 유입됐는지 추적해 보고한다.** 추적 없이
통과시키지 않는다. 초과분의 발원지를 아래 셋 가운데 어디로 보이는지 함께 적되, 분류의 확정은
결정 세션의 몫이다.

| 종류 | 예 | 성격 |
|---|---|---|
| 수 표현의 의존 | Lean core `Rat`의 기약성 증명 | 계산 가능성을 해치지 않는다 |
| 전술·인스턴스 해결의 의존 | `linarith`, 전면 `simp`가 끌어오는 `NoZeroDivisors ℤ` | 도구 구현에 있고 수학적 내용이 요구하지 않는다 |
| 논증의 의존 | `ℝ` 위의 고전적 존재 증명 | 실제 고전논리 사용 |

**두 번째 종류는 보조정리를 하나씩 감사해서는 찾지 못할 수 있다.** 유입원이 보조정리가 아니라
그 보조정리가 요구하는 **인스턴스**일 수 있기 때문이다. 실측 사례에서 `mul_eq_zero` 자체는
무의존이었고 오염은 `NoZeroDivisors ℤ` 인스턴스에 있었으며, 기준선 차분으로만 드러났다.

따라서 보조정리별 감사가 전부 깨끗하더라도 **"유입원 없음"으로 보고하지 않는다.** 차분이
초과를 가리키는데 보조정리 감사가 아무것도 잡지 못했으면, 그 사실 자체를 보고한다.
`← A-6, C-8`

### 5-3. 전면 `simp` 금지

**정의층과 회계층과 관측층에서 전면 `simp` 호출을 금지한다.** `simp only [...]`로 보조정리를
명시 고정한다. `simp_all`과 그 밖의 `simp` 변형도 같다.

근거가 둘이다. 하나는 실측이며, 전면 `simp`가 인스턴스를 통해 기준선을 오염시킨 사례가
확인됐다. 다른 하나는 구조적인 것이며, 전면 `simp`를 쓰면 **증명의 공리 의존이 소스를 읽어서는
알 수 없는 성질**이 된다. 핀된 Mathlib의 simp 집합에 의존하므로 판이 올라갈 때 조용히
달라지고, 재감사에서 깨질 때 원인을 추적하기 어렵다.

실무로는 `simp?`를 돌려 사용된 보조정리를 뽑아 고정한다.

동학층은 제외한다. `ℝ`로 이미 오염되어 있고 재유입 대상이 아니기 때문이다.

**비용을 인정한다.** 증명이 길어지고 Mathlib 갱신 시에 깨지기 쉬워진다. 그 대가로 공리 의존이
소스에서 읽히는 성질이 된다. `← C-9`

### 5-4. 변이 검사

**모든 진술**(`theorem`과 `example`)에 대해 결론을 부정한 사본이 컴파일에 실패하는지 확인한다.

두 가지 서로 다른 것을 잡으며 사유가 다르다.

- `example`은 이름이 없어 elaboration 누락이 눈에 보이지 않는다. **실제로 검사됐는지**를
  확인한다
- 명명된 정리는 컴파일되면 증명된 것이므로 누락 문제가 없다. **전술이 아무거나 증명하고 있지
  않은지**를 확인한다. `axiom`을 막아 모순 유입 경로가 없으나 값싼 보험이다

변이 사본은 원본을 건드리지 않고 임시 파일에 만들며 검사 후 삭제한다. `← A-8`

### 5-5. 음성 대조

**검사 스크립트가 공허하지 않은지 확인한다.**

각 검사 단계에 대해 그 단계가 잡아야 할 위반을 일부러 주입했을 때 실패하는지 확인한다. 통과만
보고 스크립트가 동작한다고 판단하지 않는다.

주석 안의 문자열이 코드로 오인되지 않게 한다. 검사는 주석을 제거한 뒤에 수행한다. 헤더에 적은
"`sorry` 0" 같은 설명 문구가 스스로 걸린 사례가 실제로 있었다. `← §10-1`

### 5-6. 사본 검사

빌드 타깃과 이름이 겹치는 사본 파일이 리포지터리 어디에도 없어야 한다. 각 모듈은 단 하나의
경로로만 존재한다.

검사 범위는 `crisis-framework/` 안이 아니라 저장소 전체다. 상위 디렉터리에 놓인 낡은 초안을
검토자가 열어 "검증된 파일과 손에 든 파일이 다른" 사고가 실제로 일어났고, 그때 프로젝트 판정
전부가 무효가 될 뻔했다. `← W-3`

---

## 6. 스크래치

검사와 탐색에 쓰는 코드는 `Scratch/`에 둔다. 빌드 타깃 밖이다.

```
lake env lean Scratch/Foo.lean
```

**검증된 파일을 검사 목적으로 수정하지 않는다.** 스크래치에 사본을 만들어 거기서 실험한다.

`← 자체`

---

## 7. 보고 형식

작업이 끝나면 아래를 보고한다.

1. **빌드 결과.** 파일별로 통과인지 `sorry`가 남았는지 실패인지 적는다
2. **변경 내역.** 무엇을 왜 바꾸었는지 적는다. import와 보조정리 이름과 전술을 각각 적는다
3. **공리 감사.** 정리별 `#print axioms` 출력을 적는다. 기준선 초과분이 있으면 유입원 추적과
   §5-2의 분류 소견을 함께 적는다
4. **변이 검사.** 몇 개 진술을 검사했고 전부 거부됐는지 적는다
5. **진술을 바꾸어야만 통과할 것 같은 지점.** 있으면 바꾸지 말고 지점과 이유만 적는다
6. **판단이 필요하다고 보이는 것.** 실행하지 말고 제시만 한다

**보고에 관측값과 해석을 섞지 않는다.** 관측값을 먼저 그대로 적고 해석은 따로 표시한다.

**보고에 `scripts/verify.sh`의 출력 원문과 커밋 SHA를 첨부한다.** 결정 세션은 이 리포를 손에
쥐지 않으므로 보고서가 판정의 유일한 근거가 되는데, 요약과 재서술은 근거가 되지 못한다.
출력을 줄여 적거나 "전부 통과"로 갈음하지 않는다.

**`scripts/verify.sh` 가 낸 `OUTPUT_DIGEST` 를 보고에 싣는다.** 출력을 줄여 적은 보고에서도 이
값은 싣는다. 출력 원문의 결손은 그 출력 하나만 봐서는 잡히지 않고 같은 단계의 다른 판과 맞대야
잡히는데, 해시가 그 대조를 갈음 뒤에도 가능하게 한다.

**5번 항목은 실패 기록이 아니라 산출물이다.** 결정 세션이 그것을 구멍 목록에 등재한다. 따라서
막힌 지점을 줄여 적지 않고 무엇을 시도했고 어디서 어떻게 막혔는지 적는다. `← W-3, W-5, P-5`

### 7-1. 명세의 결손을 찾았을 때

**조작을 바꾸는지로 가른다.**

| 판정 | 처분 |
|---|---|
| 조작을 바꾼다 | **멈추고 보고한다.** 고치지 않는다 |
| 조작을 바꾸지 않는다 | **고치지 말고 그대로 진행한 뒤 보고에 적는다** |

기대표가 다른 회차의 것이거나 같은 값이 두 자리에서 갈리면 앞이다. 집행이 자기 결과를 맞댈
상대가 없기 때문이며, 그 경우 정지가 옳다.

산문의 서술이 틀렸으나 조작 문면과 그 자리의 실물이 같은 값을 가리키면 뒤다. 지시서가 자기
사전 검사를 적어 둔 칸이 그 부류이며, 그 칸은 결정 세션이 무엇을 확인했는지를 적는 자리이지
집행의 조작 근거가 아니다.

**뒤로 판정해 진행한 경우에도 그 사실을 보고에 적는다.** 그 칸이 틀린 것은 결정 세션의 결손
이므로 사안으로 올린다. `← 세션 지시 §2.3`

### 7-2. 보고한 결함의 처분

**보고한 결함이 전부 원장 후보가 아니다.** 미전사분으로 세는 것은 **리포 실물에 남는 것**이며,
소진 문서 안에서만 살다 이관과 함께 사라지는 결손은 보고에 적되 미전사분에 넣지 않는다.

| 어디에 사는가 | 미전사분인가 |
|---|---|
| 리포의 추적 파일 | **그렇다** |
| 지시서 · 엔트리 카드 · 호출 프롬프트 · 보고서 | **아니다** |

**검증기가 완벽할 수 없다는 것이 이 조항의 근거다.** 검증기는 리포 안만 보고 지시서는 추적되지
않으므로 지시서의 결함은 원리상 집행이 읽어야만 잡힌다. 그것을 전부 조항으로 세우면 원장이 결함
목록이 되고, 조항이 선 것이 재발을 막지 못한다는 것도 실측됐다.

**다만 회차를 건너 살아남은 결함은 예외다.** 앞 판의 값이 새 판에 그대로 남았다면 그것은 소진
문서의 결함이 아니라 그 문서를 만드는 절차의 결함이므로 미전사분에 넣는다. `← 세션 지시 §2.3`

---

## 8. 커밋

- 한 커밋에 한 가지 변경만 담는다
- 검증을 통과한 상태만 커밋한다
- 커밋 메시지에 무엇이 검증됐는지 적는다
- `BUILD_LOG.md`를 갱신한다. 사실과 어긋난 상태로 두지 않는다

`.gitignore`는 `.lake/`를 제외한다. 재현에는 `lake-manifest.json`과 `lean-toolchain`과
`lake exe cache get`으로 충분하다. `← 자체`

---

## 9. 현재 상태

**상태의 정본은 `BUILD_LOG.md`다.** 이 문서는 상태를 다시 적지 않는다. 다른 절이 상태를 적고
있으면 그것이 드리프트이며, 고칠 곳은 여기가 아니라 그쪽이다.

`BUILD_LOG.md`는 회차별 기록이므로 **가장 뒤의 회차가 현행**이다. 앞 회차의 서술이 현행과
어긋나 보이면 그것은 결함이 아니라 그 시점의 관측이다. 소급해 고치지 않는다. `← §5.1`

**원장의 계수는 예외다.** 그 값이 딛는 것이 구현이 아니라 병합 자신이므로 여기가 정본이다.
계수는 손으로 세지 않고 `scripts/check_wiki.py` 의 출력에서 읽는다.

| 레지스터 | entry |
|---|---|
| decisions | 511 |
| deferred | 33 |
| rejected | 0 |
| retirements | 24 |

`← R-6, §5.6`

---

## 10. 모호할 때

추측하지 말고 묻는다. 이 프로젝트는 빠른 진행보다 일관성과 재현성과 감사 가능성을 우선한다.

**요약하면 이것 하나다.**

> **증명되지 않는 진술은 고칠 것이 아니라 보고할 것이다.**
>
> **검사 결과는 보고할 것이지 행동할 근거가 아니다.**

`← P-1, P-5, W-1`

---

## 11. 작업 도중 권한을 물어야 할 때

사용자는 허용하는 것으로 간주하고, 시스템에 파괴적일 수 있는 동작을 제외하고는 지시서에
명시된 정지점까지 제한 없이 작업을 진행한다.

원격 조작은 두 층으로 갈린다. push와 fetch는 회차마다 사용자 허가를 받은 뒤에 수행하고,
저장소 생성과 삭제와 가시성 전환과 remote 설정은 사용자가 전담한다. `← §6.1`

---

## 12. 정지점에 도달했을 때

사용자 측에서 프롬프트가 올라오기 전까지는 다음 작업으로 진행하지 않는다.

**정지점 보고에 계수를 적을 때는 그 계수의 유니버스와 증분의 출처를 함께 적는다.** "A가 B로
늘었다"를 적으면 무엇이 늘었는지(총계인지 내역인지)와 무엇이 그것을 늘렸는지를 같은 문장이
든다. 총계가 불변인데 내역이 옮겨 간 경우를 "늘었다"로 적지 않는다.

**산출물의 계수를 적을 때는 그 계수를 산출물에서 직접 읽는다.** 같은 이름의 값을 다른 도구로
재어 적지 않는다. job 수를 하드코딩하지 않고 빌드 로그에서 파싱하는 것이 같은 축이다.
`← §6.1, W-3`

---

## 14. 원장 병합 절차

지시서가 카드 병합을 지시할 때만 수행한다. 그 밖에는 §1-6이 금지한다.

**재료는 카드 파일이 전량 든다.** 지시서는 목적 레지스터와 병합 순서만 들고 문면은 들지 않으므로,
카드 파일에 없는 문면을 지어내지 않는다.

**순서를 지킨다.** `retirements` 의 `target` 과 `replaced_by` 가 `decisions` 와 `deferred` 의
entry 를 가리키므로 그 둘이 먼저 서야 한다.

| 순서 | 파일 | 카드 파일의 키 |
|---|---|---|
| 1 | `docs/decisions/decisions.json` | `new.decisions` |
| 2 | `docs/decisions/deferred.json` | `new.deferred` |
| 3 | `docs/decisions/retirements.json` | `new.retirements` |
| 4 | `docs/decisions/rejected.json` | (빈 레지스터) |

**병합 뒤에 등가 검증을 한다.** 네 파일에서 읽은 entry 가 카드 파일의 entry 와 **바이트로 같은지**
본다. 차분으로 갈음하지 않는다.

**등가 검증이 통과한 뒤에만 카드 파일을 지운다.** 검증의 재료가 그 시점에 실재해야 하므로
순서를 바꾸지 않는다.

**어느 단계에서든 어긋남이 나오면 고치지 말고 멈춰서 보고한다.** 원장은 수정 불가이므로 잘못
병합된 것을 되돌리는 것은 파일을 복원하는 것뿐이며, 이미 지나간 뒤에는 폐기 레코드로만 고칠 수
있다. `← R-9, W-9`

---

## 13. 한국어 서술 지침의 적용 — 프로젝트 절

아래 편입 블록은 범용 지침이고, 이 절은 그 지침을 이 프로젝트의 어느 산출물에 적용하는지를
정한다.

**적용 대상(신규 문면 전량):** ① **Lean 파일의 docstring과 주석 전량** ② `BUILD_LOG.md`와
`docs/` 상주 문서에 새로 쓰는 문면 ③ 사용자에게 하는 보고와 정지점 보고.

**편입 블록 「동작 범위」 (2)를 이 프로젝트에서 덮는다.** 원안은 주석을 제외 대상으로 두었으나
이 프로젝트는 주석과 docstring을 적용 대상으로 삼는다. 근거는 둘이다. 하나는 docstring의 필수
3항목이 이 프로젝트의 1차 산출물이며 사람이 읽는 논증이라는 점이고, 다른 하나는 주석의 의미가
흐리면 그 흐림이 그대로 판정의 흐림이 된다는 점이다. **식별자와 커밋 메시지는 (2)의 원안대로
제외한다.**

**소급하지 않는다**(「동작 범위」 (5)항). 이미 작성된 docstring과 `BUILD_LOG.md`의 앞 회차
기록은 고치지 않으며, 다른 사유로 그 문면을 편집하게 될 때 함께 정리한다. 지침 적용 시점을
전후로 문체가 갈리는 것은 결함이 아니라 의도한 결과다.

**제외 대상을 여기서 다시 나열하지 않는다.** 편입 블록의 「동작 범위」 절이 정본이며, 두 곳에
같은 목록을 두면 그 둘이 갈라진다. `← §7.1`

---

아래 블록은 그 블록 자신이 요약을 금지하므로 문면을 고치지 않고 그대로 둔다.

---------------------------------------------------

의미가 명확한 한국어 문장을 출력하게 하는 지침:
한국어를 활용해야 하는 상황이라면 아래에 제시된 지침들을 준수하여 의사 소통의 효율성을 높일 것. 이 지침들은, 의미가 명확하며 비교적 가독성이 높고 안정적인 구조를 지닌 한국어 문장을 출력하는 방법을 자세히 설명함.

## 상황과 목표

- LLM은 한국어를 구사할 때 몇 가지 특징을 보이는데, 일부 특징은 결과물의 완성도를 낮추거나, 사용자가 소통에 더 많은 노력을 들이게 만듬. 이 문서에 작성된 사항들을 준수하면 이런 현상을 개선할 수 있음.
- 이 문서에서 제시하는 지침들을 요약하는 것은 일반적으로 권장되지 않음. 그렇게 한다면 조항마다 첨부된 예시를 확인할 수 없으므로 조항의 문구가 구체적으로 어떤 동작을 의도했는지 파악하기 어려워짐. 또한 요약에 포함된 몇 가지 지침을 제외한 나머지 지침들은 잘 준수되지 않는 방향으로 서술 압력이 작동하게 될 수도 있음. 그리고 목적과 의도를 생략하고 제한 사항만 요약한다면 목적에 부합하지 않게 기계적으로 지침을 준수했는지 확인하게 될 수도 있음.

## 동작 범위

(1) 본문의 지침들은 한국어를 활용하는 상황에서 그 한국어를 명확하게 출력하라는 지시임. 외국어 문장이나 어휘를 출력해야 하는 상황에서, 그것을 한국어로 번역하거나 대체하라는 지시가 아님.
(2) 변수명과 주석, 커밋 메시지, 로그 문자열처럼 코드에 속하는 텍스트는 프로젝트의 기존 관례를 준수해야 함. 이러한 텍스트는 지침을 적용하면 안 되기 때문에 이 조항에서 한 번 더 강조함.
(3) 고유 명사와 기술 용어 등은, 통상적인 용례로 정착된 번역어 혹은 음차가 있다면 우선적으로 사용하고, 그렇지 않다면 원어를 유지함으로써, 한국어 사용자가 이해하기 편하고 의미를 잘 이해할 수 있도록 할 것.
(4) 사용자가 어떤 어조나 어휘를 사용하든지, 사용자 메시지의 어조를 모방하지 않고, 본문에서 제시하는 지침들을 일관되게 유지할 것.
(5) 이 지침들은 앞으로 작성하는 문면에 적용되고, 이미 작성된 문면을 소급하여 고치는 근거가 되지 않음. 기존 문면은 그것을 다른 사유로 편집하게 될 때 함께 정리하면 됨. 이렇게 하는 이유는, 기록으로 남긴 문면을 나중에 고치면 그 기록이 작성된 시점에 무엇을 알고 있었는지를 확인할 수 없게 되기 때문임. 따라서 지침을 적용하기 시작한 시점을 전후로 문체가 갈리는 것은 결함이 아니라 이 방침이 의도한 결과임.
(6) 인용문에는 이 지침들을 적용하지 않음. 다른 문서나 다른 사람의 표현을 그대로 옮기는 것이 인용의 목적이므로, 인용한 문면의 문체를 이 지침에 맞추어 고치면 원문이 무엇이었는지 확인할 수 없게 됨. 원장의 기존 문면을 옮겨 적는 경우와, 조작 지점을 특정하려고 현행 문면 조각을 그대로 드는 경우가 모두 인용에 해당함.

## 문장 단위

(1) 읽는 이가 문장의 의미를 충분히 이해할 수 있어야 하므로, 의미가 있는 문장 성분을 생략하지 않아야 함. [그러면 경고가 붙습니다.→ ('그러면 이미 작업중인 파일에도 경고 표지가 추가됩니다.'와 같이, 맥락과 정보를 충분히 제공하도록 수정) ]  특히 보조사 '의'를 필요 이상으로 사용한다면, 의미를 담고 있는 문장 성분을 생략하기 쉬우므로 유의할 것.  [사본의 문구는 작업의 상황을 → 사본에 기재된 문구는 작업이 진행되는 상황을]
(2) (이 2번 조항은 헤더와 목록에는 강제로 적용되는 사항이 아님.) 명사구나 부사구, 연결어미로 문장을 끝내지 말고, 서술어와 종결어미를 사용하여 완성된 형태의 문장으로 끝을 맺어야 함.

## 구 단위

(1) 필수적인 경우가 아니라면 조사와 어미를 생략하지 말 것. 또한 부사, 보조사와 선어말어미, 보조 용언을 적극적으로 활용하면, 의미가 명확한 한국어 문장을 완성할 수 있음. [이 결정은 이후 중요 정책이 갈리는 자리. 컨텍스트 압축 전 신중 반영한다. → 이 결정은 이후 중요한 정책에 지속적으로 영향을 주기 때문에, 컨텍스트가 압축되기 전에 신중히 반영합니다. → 지금 답변해주신 결정 사항은 이후 중요한 정책에도 지속적으로 영향을 미치기 때문에, 컨텍스트가 압축되기 전에 미리 신중하게 반영해 놓겠습니다.]
(2) 구체적인 의미를 담고 있는 한자어와 자연스러운 통사 구조를 결합하면, 풍부하고 명확한 의미를 전달할 수 있으므로 맥락에 적합한 한자어를 적극적으로 활용하고, 그 한자어에 조사와 어미를 붙여서 어휘 사이의 관계를 확실하게 나타내야 함. [<쓴 비용을 구하는 토큰 카운트 함수에 문제가 생기면 (상황에 적합한 어휘가 사용되지 않아 의미가 불충분함) /지출 비용 추론 용도의 토큰 카운트 함수의 오류 상황에서 (조사와 어미가 없어 가독성이 낮고 의미 관계가 불분명함)>  → 지출한 비용을 추론하는 토큰 카운트 함수에 오류가 발생하면 (이 지침의 목표 예시)]
(3) 일반적인 어휘를 사용해야 하는 자리에 비유적 어휘를 사용하면 가독성이 낮고, 의미가 변질되기 쉽기 때문에, 꼭 필요한 경우가 아니라면 비유적 어휘로 일반적인 명사나 동사를 대체하지 말 것. 다만 일상적인 문어에서 통용되고 지금 다루는 분야에서도 관용 표현으로 정착되어 있어서, 일반적인 어휘로 바꾸면 오히려 어색해지는 표현은 그대로 사용할 수 있음. [<분석의 흐름 → 분석의 방향성>, <코드로 박는 자리 → 코드에 명시하는 상황 (혹은 코드에 명시하는 작업)>, <요청을 받습니다 -> 요청을 확인했습니다 (혹은 요청대로 수행하겠습니다)>]
(4) 엠대시(—)는 앞뒤 문장의 관계를 지나치게 함축하기 때문에 자제하고, 문맥과 형식에 따라 콜론이나 접속사로 대체할 것. 다만 산문 문장 안에서 쓰는 경우와, 표제와 그 내용을 가르는 자리에서 쓰는 경우를 구별해야 함. 전자는 두 문장 사이의 논리적 관계를 함축하여 숨기므로 그 관계를 드러내는 접속사나 어미로 대체해야 하지만, 후자는 관계가 아니라 구획을 표시하는 것이므로 그대로 두어도 의미가 흐려지지 않기 때문임. 목록과 표의 항목 안에서도 같은 기준을 적용할 것. [<이 결정은 후속 판정을 구속한다 — 근거가 죽기 때문이다.> → <이 결정은 근거가 죽기 때문에 후속 판정을 구속합니다.> (산문 문장 안이므로 관계를 드러내는 어미로 대체)] [<**④ 기각 3종** — 보고국만 인정한다> (표제와 내용을 가르는 자리이므로 유지)] 또한 프로젝트에서 정형구로 굳어진 표현은 엠대시를 포함하더라도 그 형태를 유지할 것. 정형구를 자리마다 다르게 풀어 쓰면 같은 뜻을 지닌 문면인지 기계적으로 대조하기가 어려워지기 때문임.

## 추가 사항

서브에이전트를 호출할 때, 한국어로 프롬프트를 작성했다면 실제로 서브에이전트 호출 도구를 사용하기 전에 이 본문의 지침들이 준수되어 있는지 점검할 것. 서브에이전트가 산출한 결과를 사용자에게 전달할 때에도 본문의 지침들이 그대로 적용될 것임.